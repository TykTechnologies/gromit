# Generated by: gromit policy

# Check the documentation at http://goreleaser.com
# This project needs CGO_ENABLED=1 and the cross-compiler toolchains for
# - arm64
# - amd64
version: 2
builds:
  - id: std-amd64
    env:
      - NOP=nop # ignore this, it is jsut to avoid a complex conditional in the templates
    ldflags:
      - -X github.com/TykTechnologies/midsommar/main.Version={{.Version}}
      - -X github.com/TykTechnologies/midsommar/main.Commit={{.FullCommit}}
      - -X github.com/TykTechnologies/midsommar/main.BuildDate={{.Date}}
      - -X github.com/TykTechnologies/midsommar/main.BuiltBy=goreleaser
    goos:
      - linux
    goarch:
      - amd64
    binary: midsommar
  - id: std-arm64
    env:
      - NOP=nop # ignore this, it is jsut to avoid a complex conditional in the templates
    ldflags:
      - -X github.com/TykTechnologies/midsommar/main.Version={{.Version}}
      - -X github.com/TykTechnologies/midsommar/main.Commit={{.FullCommit}}
      - -X github.com/TykTechnologies/midsommar/main.BuildDate={{.Date}}
      - -X github.com/TykTechnologies/midsommar/main.BuiltBy=goreleaser
    goos:
      - linux
    goarch:
      - arm64
    binary: midsommar
  - id: std-s390x
    env:
      - NOP=nop # ignore this, it is jsut to avoid a complex conditional in the templates
    ldflags:
      - -X github.com/TykTechnologies/midsommar/main.Version={{.Version}}
      - -X github.com/TykTechnologies/midsommar/main.Commit={{.FullCommit}}
      - -X github.com/TykTechnologies/midsommar/main.BuildDate={{.Date}}
      - -X github.com/TykTechnologies/midsommar/main.BuiltBy=goreleaser
    goos:
      - linux
    goarch:
      - s390x
    binary: midsommar
nfpms:
  - id: std
    vendor: "Tyk Technologies Ltd"
    homepage: "https://tyk.io"
    maintainer: "Tyk <info@tyk.io>"
    description: Tyk AI Portal
    package_name: midsommar
    file_name_template: "{{ .ConventionalFileName }}"
    ids:
      - std-amd64
      - std-arm64
      - std-s390x
    formats:
      - deb
      - rpm
    contents:
      - src: "README.md"
        dst: "/opt/share/docs/midsommar/README.md"
      - src: "ci/install/*"
        dst: "/opt/midsommar/install"
      - src: ci/install/inits/systemd/system/midsommar.service
        dst: /lib/systemd/system/midsommar.service
      - src: ci/install/inits/sysv/init.d/midsommar
        dst: /etc/init.d/midsommar
    scripts:
      preinstall: "ci/install/before_install.sh"
      postinstall: "ci/install/post_install.sh"
      postremove: "ci/install/post_remove.sh"
    bindir: "/opt/midsommar"
    rpm:
      scripts:
        posttrans: ci/install/post_trans.sh
      signature:
        key_file: tyk.io.signing.key
    deb:
      signature:
        key_file: tyk.io.signing.key
        type: origin
publishers:
  - name: std
    ids:
      - std
    env:
      - PACKAGECLOUD_TOKEN={{ .Env.PACKAGECLOUD_TOKEN }}
    cmd: packagecloud publish --debvers "{{ .Env.DEBVERS }}" --rpmvers "{{ .Env.RPMVERS }}" tyk/midsommar {{ .ArtifactPath }}
# This disables archives
archives:
  - formats: ['binary']
    allow_different_binary_count: true
checksum:
  disable: true
release:
  disable: true
  github:
    owner: TykTechnologies
    name: midsommar
  prerelease: auto
  draft: true
  name_template: "{{.ProjectName}}-v{{.Version}}"
